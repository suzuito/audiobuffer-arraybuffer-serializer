{
  "name": "audiobuffer-arraybuffer-converter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && mkdir dist && rollup --config",
    "test": "npm run build && nyc mocha --recursive",
    "test-report": "npm run test && nyc report --reporter=lcov --reporter=text"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=8.0.0"
  },
  "devDependencies": {
    "browserify": "^15.1.0",
    "chai": "^4.1.2",
    "eslint": "^4.15.0",
    "http-server": "^0.11.1",
    "mocha": "^4.1.0",
    "nyc": "^11.4.1",
    "rollup": "^0.54.0",
    "sinon": "^4.1.4"
  },
  "dependencies": {
    "audio-buffer": "^4.0.4"
  }
}
